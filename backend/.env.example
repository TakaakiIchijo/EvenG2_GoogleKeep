# Keep G2 バックエンドサーバー 環境変数設定
# このファイルを .env にコピーして値を設定してください
# .env は .gitignore に含まれているため、リポジトリにコミットされません

# Google アカウントのメールアドレス
KEEP_EMAIL=your-email@gmail.com

# Google Master Token（aas_et/... 形式）
# 取得方法は README.md の「Master Token の取得」セクションを参照
KEEP_MASTER_TOKEN=aas_et/...

# ノートキャッシュファイルのパス（省略可）
# KEEP_STATE_FILE=keep_state.json

# サーバーポート（省略可、デフォルト: 8080）
# PORT=8080

# フロントエンドのオリジン（GitHub Pages などの本番環境で設定）
# FRONTEND_ORIGIN=https://your-username.github.io
